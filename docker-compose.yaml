services:
  web: 
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./backend:/var/www/html
      - ./.develop/nginx.conf:/etc/nginx/conf.d/default.conf
    networks: 
      - psychology-network
  
  php-fpm:
    build: .
    container_name: laravel
    image: php:8.2.12-fpm
    env_file:
      - ./.env
    volumes:
      - ./backend:/app
    networks: 
      - psychology-network

  db:
    image: mysql:9.5.0-oraclelinux9
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: psychologist_db
      MYSQL_USER: psychologist_user
      MYSQL_PASSWORD: psychology_pass
    volumes:
      - mysql_data:/var/lib/mysql
      - ./.develop/mysql_init:/docker-entrypoint-initdb.d
    networks:
      - psychology-network
    
  adminer:
    image: adminer:4.8.1
    ports:
      - "8080:8080"
    environment:
      ADMINER_DEFAULT_SERVER: mysql
      ADMINER_DEFAULT_USER: app_user
      ADMINER_DEFAULT_HOST: db
      ADMINER_DESIGN: dracula
    networks:
      - psychology-network
    depends_on:
      - db

networks:
  psychology-network:
    driver: bridge

volumes:
  mysql_data: